const ROLES = require('./roles.js');

//temp
let playerUnit = {
  leader: ROLES['general'],
  units: [ROLES['guardsmen(trooper)'], ROLES['guardsmen(trooper)'], ROLES['guardsmen(trooper)']],
}

module.exports = playerUnit;